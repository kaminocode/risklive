[pytest]
addopts = -q --cov=src --cov-report=term-missing --cov-fail-under=100
markers =
    unit: fast isolated unit test
    integration: broader integration test without external network
    regression: archived dataset regression and contract tests

testpaths =
    tests

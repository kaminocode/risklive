# Use a domain when available (recommended), e.g. dashboard.example.com
# For HTTP IP/port testing only, use http://<ip>:<port> in Caddyfile or set a local host.
CADDY_SITE_ADDRESS=localhost

OPS_USER=opsadmin
# Generate with:
# caddy hash-password --plaintext 'your-strong-password'
OPS_PASSWORD_HASH=replace_me
